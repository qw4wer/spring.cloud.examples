server:
  port: 5424

spring:
  application:
    name: nacos-consumer
  cloud:
    nacos:
      discovery:
        server-addr: 10.0.0.20:8848
      config:
        server-addr: 10.0.0.20:8848

feign:
  circuitbreaker:
    enabled: true